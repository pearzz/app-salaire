#- name: create user
 # hosts: [all]
  #roles:
   # - create_user
    
- name: install httpd
  become_user: app-salaire
  hosts: [web]
  roles:
    - httpd
    
- name: install mariadb
  become_user: app-salaire
  hosts: [db]
  roles:
    - mariadb
